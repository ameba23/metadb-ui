// const axios = require('axios')
//
// module.exports = function createRequest ({ host, port, timeout, basicAuthUser, basicAuthPassword }) {
//   return axios.create({
//     baseURL: `${host}:${port}/`,
//     timeout: timeout || 10000,
//     headers: { 'Content-type': 'application/json' },
//     auth: (basicAuthUser && basicAuthPassword)
//       ? { username: basicAuthUser, password: basicAuthPassword }
//       : undefined
//   })
// }
