{
  "name": "metadb-ui",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "browserify index.js -t sheetify -o bundle.js",
    "test": "standard && npm run test-deps",
    "test-deps": "dependency-check . && dependency-check . --extra --no-dev -i tachyons"
  },
  "dependencies": {
    "axios": "^0.19.0",
    "choo": "^6.13.3",
    "request": "^2.88.0",
    "sheetify": "^7.4.0",
    "tachyons": "^4.11.1"
  },
  "devDependencies": {
    "dependency-check": "^3.3.0",
    "standard": "^12.0.1"
  }
}
